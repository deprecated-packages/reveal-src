{% for item in items %}
    {{ item.some_method }}
{% endfor %}
-----
<?php

// line 1
$context['_parent'] = $context;
foreach ($items as $context["_key"] => $item) {
    // line 2
    echo "    ";
    echo $item->some_method();
    echo "\n";
}
